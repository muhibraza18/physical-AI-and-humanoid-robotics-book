name: Deploy Docusaurus to GitHub Pages

on:
  push:
    branches:
      - main # Or your default branch
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: npm
          
      - name: Install dependencies
        run: npm install
        
      - name: Build Docusaurus website
        run: npm run build
        
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          publish_branch: gh-pages

  # Placeholder for FastAPI Backend Deployment
  # This would typically be a separate job or additional steps
  # in the existing job, depending on backend hosting.
  # For Vercel, this might involve configuring a vercel.json in the root
  # to detect the rag-backend/ directory as a separate project.
  # deploy-backend:
  #   name: Deploy FastAPI Backend
  #   runs-on: ubuntu-latest
  #   needs: deploy # Assuming it deploys after frontend
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #     - name: Deploy to Vercel (or other platform)
  #       run: |
  #         # Commands to deploy your FastAPI backend
  #         # e.g., if using Vercel CLI: vercel deploy --prebuilt --prod
  #         echo "FastAPI backend deployment steps go here"